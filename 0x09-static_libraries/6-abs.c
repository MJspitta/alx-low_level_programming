#include "main.h"

/**
 *_abs - Check description
 * @n: integer input
 * Description: return absolute value of a number
 * Return: Absolut value of number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
